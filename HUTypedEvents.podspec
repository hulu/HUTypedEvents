Pod::Spec.new do |s|
  s.name         = "HUTypedEvents"
  s.version      = "1.0.0"
  s.summary      = "An alternative way to manage events in Objective C."
  s.requires_arc = true
  s.author       = "Hulu"
  s.platform     = :ios
  s.source_files = 'src'
end
